<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/content_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="com.thecoldheartofspace.karl.bluetoothbutton.MainActivity"
    tools:showIn="@layout/activity_main">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Hello World!"
        android:id="@+id/textView" />

    <Button
        android:text="Make Discoverable"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/textView"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_marginLeft="52dp"
        android:layout_marginStart="52dp"
        android:layout_marginTop="84dp"
        android:id="@+id/makeDiscoverableButton"
        android:elevation="0dp"
        android:onClick="discoverableWasClicked" />

    <Button
        android:text="Panic"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/makeDiscoverableButton"
        android:layout_alignRight="@+id/makeDiscoverableButton"
        android:layout_alignEnd="@+id/makeDiscoverableButton"
        android:layout_marginTop="21dp"
        android:id="@+id/panicButton"
        android:layout_alignLeft="@+id/makeDiscoverableButton"
        android:layout_alignStart="@+id/makeDiscoverableButton"
        android:onClick="panicWasClicked" />
</RelativeLayout>
